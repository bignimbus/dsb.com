.main-nav {
    text-align: center;
    ul {
        cf();
    }
    li {
        padding: 1rem 0;
        position: relative;
        col(1/6, gutter: 0, cycle: 6);
        +mobile() {
            col(1, uncycle: 6, cycle: 1)
        }
        a {
            color: inherit;
            text-decoration: none;
            &:before,
            &:after {
                background: url(/assets/images/dsb_chili.png) no-repeat;
                background-size: contain;
                content: '';
                display: inline-block;
                height: 1rem;
                opacity: 0;
                transition: opacity .3s;
                width: 1rem;
            }
            &:hover {
                &:before,
                &:after {
                    opacity: 1;
                }
            }
        }
    }
}