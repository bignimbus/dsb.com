.track {
    col(1/2, cycle: 2);
    .cover {
        col(1/3, cycle: 2);
    }
    .description {
        col(2/3, cycle: 2);
    }
    +mobile() {
        col(1, cycle: 1, uncycle: 2);
        .cover, .description {
            col(1, cycle: 1, uncycle: 2);
        }
    }
}